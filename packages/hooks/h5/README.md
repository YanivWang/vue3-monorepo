# @vue3-mono/hooks-h5

依赖 Vant 的 H5 专用 hooks

## 清单（P5.2 填充）

- `useToast`：`showToast` 封装
- `useDialog`：`showDialog` 封装
- `useSafeArea`：iOS 安全区
- `useViewport`：viewport / DPR 辅助

## 构建

`unbuild` → `esm only` + `.d.ts`；`vue` / `vant` / `@vue3-mono/hooks` 走 externals
