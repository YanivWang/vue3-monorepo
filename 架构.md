# 架构说明

> 本文档与代码保持同步，详细在线版见 [docs/guide/architecture.md](docs/guide/architecture.md)。

## 技术栈

| 分类     | 技术                        | 版本  |
| -------- | --------------------------- | ----- |
| 框架     | Vue 3 (Composition API)     | ^3.4  |
| 构建     | Vite                        | ^5.3  |
| 语言     | TypeScript（严格模式）      | ^5.5  |
| UI       | Element Plus                | ^2.7  |
| 状态管理 | Pinia                       | ^2.1  |
| 路由     | Vue Router                  | ^4.3  |
| HTTP     | Axios（自定义封装）         | ^1.7  |
| 国际化   | Vue I18n                    | ^9.14 |
| 工具     | VueUse / Lodash-ES / Day.js | —     |

## 目录结构

```
├── .github/
│   └── workflows/
│       ├── ci.yml           # 代码质量：lint + type-check + test + build
│       └── deploy.yml       # CD：docker build/push + SSH 部署
├── docs/                    # VitePress 文档站（pnpm docs:dev）
│   ├── .vitepress/config.ts
│   ├── guide/               # 快速上手、架构、权限、主题、部署
│   └── components/          # 组件文档
├── mock/                    # vite-plugin-mock 接口数据（开发用）
│   ├── user.ts
│   └── menu.ts
├── src/
│   ├── api/                 # 接口层
│   │   ├── index.ts         # 统一导出
│   │   └── modules/         # 按模块拆分
│   ├── assets/styles/       # 全局样式
│   │   ├── variables.scss   # SCSS 变量 + CSS 自定义属性（:root）
│   │   ├── dark.scss        # 暗黑模式 CSS 变量覆盖（html.dark）
│   │   ├── reset.scss       # CSS Reset
│   │   └── index.scss       # 入口（引入 reset + dark）
│   ├── components/          # 通用业务组件
│   │   ├── ErrorBoundary/   # 错误边界（onErrorCaptured）
│   │   ├── PageContainer/   # 页面卡片容器
│   │   ├── ProTable/        # 高级表格（分页+搜索+多选）
│   │   └── SvgIcon/         # SVG 图标
│   ├── composables/         # 组合式函数
│   │   ├── usePermission.ts # 权限判断（hasPermission/hasRole）
│   │   └── useTable.ts      # 表格分页逻辑
│   ├── directives/          # 自定义指令
│   │   ├── index.ts         # 统一注册入口
│   │   ├── permission.ts    # v-permission（权限码控制）
│   │   └── role.ts          # v-role（角色控制）
│   ├── enums/               # 枚举定义
│   ├── locales/             # 国际化（zh-CN / en-US）
│   ├── plugins/             # 插件注册
│   │   ├── index.ts         # setupPlugins 入口
│   │   ├── element-plus.ts  # 图标全量注册
│   │   ├── errorHandler.ts  # 全局异常处理
│   │   └── (i18n → locales/index.ts)
│   ├── router/
│   │   ├── index.ts         # 静态路由 + resetRouter
│   │   └── guards.ts        # 路由守卫（白名单/权限/Tab）
│   ├── stores/
│   │   ├── index.ts         # setupStore 入口
│   │   └── modules/
│   │       ├── app.ts       # 主题/语言/侧边栏
│   │       ├── user.ts      # 登录/用户信息/token
│   │       ├── permission.ts# 菜单/动态路由
│   │       └── tabs.ts      # 标签页
│   ├── types/               # TypeScript 类型声明
│   │   ├── api.d.ts
│   │   ├── env.d.ts
│   │   ├── global.d.ts
│   │   └── router.d.ts      # RouteMeta 扩展
│   ├── utils/
│   │   ├── http/            # Axios 封装（AbortController/刷新/重试）
│   │   ├── common.ts
│   │   ├── storage.ts
│   │   └── performance.ts   # Web Vitals 采集
│   └── views/               # 页面
│       ├── layout/          # 布局（Header/Sidebar/Tabs/Main）
│       ├── home/
│       ├── login/
│       ├── auth/            # 注册/忘记密码
│       └── error/           # 403/404
├── Dockerfile               # 多阶段构建：node build → nginx serve
├── nginx.conf               # SPA路由/gzip/安全头/长效缓存
├── docker-compose.yml       # 本地验证
├── .release-it.json         # 自动版本管理（Conventional Commits）
├── CHANGELOG.md             # 版本变更记录
└── CONTRIBUTING.md          # 贡献指南
```

## 启动流程（main.ts）

```
bootstrap()
  1. setupStore(app)          — 注册 Pinia
  2. registerDirectives(app)  — 注册 v-permission / v-role
  3. setupPlugins(app)
     └─ setupErrorHandler     — Vue 错误 / window.onerror / unhandledrejection
     └─ setupElementPlus      — 图标全量注册
     └─ setupI18n             — vue-i18n
  4. app.use(router)
  5. await router.isReady()   — 等待路由守卫执行
  6. app.mount('#app')
  7. initWebVitals()          — CLS/INP/LCP/FCP/TTFB 监控
```

## 路由流程

```
进入路由
  ↓
白名单？→ 直接放行
  ↓ 否
有 token？
  否 → redirect /login
  是 ↓
路由已加载？
  否 → fetchUserInfo() + generateRoutes() → addRoute → replace 重匹配
  是 ↓
meta.permissions / meta.roles 检查
  失败 → /403
  成功 → next()
afterEach: setTitle + tabsStore.addTab
```

## HTTP 层特性

- **Token 自动刷新**：401 → 暂停请求队列 → 刷新 → 重放
- **请求取消**：`cancelDuplicate: true` + AbortController 请求池
- **自动重试**：服务端 5xx 错误，指数退避重试
- **统一错误提示**：ElMessage，可通过 `showError: false` 关闭
- **全局 Loading**：`showLoading: true` 触发

## 权限体系

| 层级             | 机制                                                    |
| ---------------- | ------------------------------------------------------- |
| 页面级           | `meta.permissions` / `meta.roles` + 路由守卫            |
| 按钮级（权限码） | `v-permission` 指令 / `usePermission().hasPermission()` |
| 按钮级（角色）   | `v-role` 指令 / `usePermission().hasRole()`             |
| 超级管理员       | `permissions` 包含 `*:*:*` 时跳过所有权限检查           |

## 暗黑模式

- `appStore.setTheme('light'|'dark'|'system')` 切换
- `system` 模式监听 `prefers-color-scheme` 媒体查询
- 基于 CSS 自定义属性（`:root` / `html.dark`）
- Element Plus 官方 `dark/css-vars.css` 集成

## CI/CD

```
push to main/master
  ├─ ci.yml: pnpm lint + lint:style + type-check + test:run + build
  └─ deploy.yml (needs: check):
       docker build → push to registry → SSH → docker run
```

## 代码规范

- ESLint 9 扁平配置（typescript-eslint + eslint-plugin-vue + prettier）
- Stylelint（standard-scss + order + BEM 类名规则）
- Prettier（printWidth:120 / semi:false / singleQuote:true）
- Commitlint（conventional + type-enum 含 wip / header-max-length:100）
- Husky：pre-commit → lint-staged；commit-msg → commitlint
