# 架构说明

> 本文档与代码保持同步；更细的「在线版」与组件说明见 [docs/guide/architecture.md](docs/guide/architecture.md) 与 [docs/](docs/)。

## 项目定位

单仓库、单包（`package.json` 根级）的 **Vue 3 + Vite + TypeScript** 企业级前端应用模板，使用 **pnpm** 管理依赖。业务代码集中在 `src/`，配套 **VitePress 文档站**、**OpenAPI 类型生成**、**GitHub Actions CI/CD** 与 **Docker + Nginx** 部署示例。

---

## 技术栈总览

### 运行时依赖（`dependencies`）

| 分类                   | 技术                                | 版本                            |
| ---------------------- | ----------------------------------- | ------------------------------- |
| 核心                   | Vue 3（Composition API）            | `^3.4.31`                       |
| 构建时注入、运行时同构 | 见下方「Vite 插件与自动导入」       | —                               |
| 路由                   | Vue Router                          | `^4.3.3`                        |
| 状态                   | Pinia                               | `^2.1.7`                        |
| UI 框架                | Element Plus                        | `^2.7.6`                        |
| 图标                   | @element-plus/icons-vue             | `^2.3.1`                        |
| HTTP                   | Axios                               | `^1.7.2`                        |
| 国际化                 | Vue I18n                            | `^9.14.4`                       |
| 组合式工具             | @vueuse/core                        | `^10.11.0`                      |
| 工具库                 | lodash-es、dayjs、js-cookie         | 见 [package.json](package.json) |
| 进度条                 | nprogress                           | `^0.2.0`                        |
| 图表                   | ECharts、vue-echarts                | `^6.0.0` / `^8.0.1`             |
| 可观测性               | Sentry（`@sentry/vue`）、web-vitals | `^10.49.0` / `^5.2.0`           |

> **持久化**：路由级状态持久化由 `pinia-plugin-persistedstate` 在 `src/stores/index.ts` 注册；该包在 [package.json](package.json) 的 `devDependencies` 中，与打包结果仍会包含持久化逻辑。

### 开发期依赖与工具（`devDependencies` 摘要）

| 分类                | 技术                                                                                                              | 说明                                                                                                              |
| ------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| 语言                | TypeScript                                                                                                        | `^5.5.3`，应用配置见 [tsconfig.app.json](tsconfig.app.json)（`strict: true` 等）                                  |
| 类型检查（Vue SFC） | vue-tsc                                                                                                           | 与 `build`、`type-check` 脚本联用                                                                                 |
| 构建                | Vite                                                                                                              | `^5.3.3`                                                                                                          |
| Vue 单文件支持      | @vitejs/plugin-vue                                                                                                | 与 Vite 配套                                                                                                      |
| 自动导入            | unplugin-auto-import、unplugin-vue-components + ElementPlusResolver                                               | 生成 `src/types/auto-imports.d.ts`、`.eslintrc-auto-import.json`、`components.d.ts`                               |
| Mock                | vite-plugin-mock                                                                                                  | `VITE_USE_MOCK=true` 时启用，目录 [mock/](mock/)                                                                  |
| 压缩产物            | vite-plugin-compression2                                                                                          | gzip / brotli                                                                                                     |
| 包分析              | rollup-plugin-visualizer                                                                                          | `VITE_ANALYZE=true` 时 `vite build` 生成 `dist/stats.html`                                                        |
| 样式                | sass（现代 API）、各 stylelint 相关包                                                                             | SCSS 全局注入见 [vite.config.ts](vite.config.ts)（`@use` variables）                                              |
| 持久化插件          | pinia-plugin-persistedstate                                                                                       | 见上文                                                                                                            |
| 单元测试            | Vitest、@vue/test-utils、happy-dom、@vitest/coverage-v8                                                           | 配置 [vitest.config.ts](vitest.config.ts)                                                                         |
| 代码质量            | ESLint 9（flat config）、typescript-eslint、eslint-plugin-vue、vue-eslint-parser、eslint-config-prettier、globals | [eslint.config.mjs](eslint.config.mjs)                                                                            |
| 样式质量            | Stylelint 17 及 config-standard、scss、order、html、recess-order、stylelint-prettier 等                           | [.stylelintrc.json](.stylelintrc.json)                                                                            |
| 格式化              | Prettier                                                                                                          | [prettier.config.mjs](prettier.config.mjs)                                                                        |
| Git 钩子            | Husky、lint-staged                                                                                                | `prepare` 安装 husky；[.husky/pre-commit](.husky/pre-commit) 跑 lint-staged                                       |
| 提交规范            | @commitlint/cli、config-conventional                                                                              | [commitlint.config.ts](commitlint.config.ts)                                                                      |
| 发布                | release-it、@release-it/conventional-changelog                                                                    | [.release-it.json](.release-it.json)，生成 CHANGELOG、打 tag、GitHub Release（npm 不发布）                        |
| API 类型生成        | openapi-typescript、tsx                                                                                           | [scripts/gen-api.ts](scripts/gen-api.ts)，从 `openapi/api.yaml` 或 `OPENAPI_URL` 生成 `src/types/api-schema.d.ts` |
| 文档站              | VitePress                                                                                                         | [docs/.vitepress/config.ts](docs/.vitepress/config.ts)                                                            |
| 类型定义            | @types/node、@types/lodash-es、@types/nprogress、@types/js-cookie                                                 | —                                                                                                                 |

### 包管理与运行时

| 项             | 值                                                                                                      |
| -------------- | ------------------------------------------------------------------------------------------------------- |
| 包管理器       | **pnpm** `10.33.1`（[package.json](package.json) `packageManager` 字段，CI 用 `pnpm/action-setup`）     |
| 建议 Node 版本 | 与 **CI / Docker 一致为 Node 20**（.github `setup-node@20`、[Dockerfile](Dockerfile) `node:20-alpine`） |
| 模块           | ESM（`"type": "module"`）                                                                               |

### Vite 插件与工程能力

- **路径别名**：`@` → `src`（[vite.config.ts](vite.config.ts)）。
- **开发代理**：`VITE_API_PREFIX`（默认 `/api`）代理到 `VITE_API_BASE_URL`（默认 `http://localhost:3000`），并 `rewrite` 去掉前缀。
- **SCSS**：`modern-compiler` + 全局 `additionalData` 引入 `variables.scss`。
- **生产构建**：`target: es2015`；`manualChunks` 拆分 `element-plus`、`vue-vendor`、`vue-i18n`、`utils`；`VITE_SOURCEMAP` 控制 sourcemap；构建后配合 compression 插件产出压缩资源。
- **环境变量类型**：`src/types/env.d.ts`（`VITE_*` 如 Mock、Sentry、分析、Web Vitals 上报等）。

### 测试与覆盖率

- **运行器**：Vitest，**环境**：happy-dom，**全局 API**：`globals: true`。
- **文件**：`src/**/*.spec.ts`、`src/**/*.test.ts`。
- **覆盖率**（@vitest/coverage-v8）：`coverage/` 下 text/json/html/lcov，阈值 `lines/functions/statements` 60%、`branches` 50%；排除 `src/types/**`、`src/main.ts`、`.d.ts`、`src/views/examples/**` 等（见 [vitest.config.ts](vitest.config.ts)）。

### 质量与 Git 工作流

| 项          | 说明                                                                                                                                                  |
| ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| ESLint      | 忽略 `dist`、`docs/.vitepress/dist`、部分生成的 `.d.ts`；Vue + TS 推荐规则；unplugin-auto-import 的全局量来自 `.eslintrc-auto-import.json`            |
| Stylelint   | 针对 `src/**/*.scss`、`src/**/*.vue`                                                                                                                  |
| Prettier    | 与 ESLint/Stylelint 通过各自 prettier 集成配合                                                                                                        |
| lint-staged | `*.{ts,vue,js,mjs}` ESLint；样式 Stylelint；多类文件 Prettier（见 [package.json](package.json) `lint-staged`）                                        |
| pre-commit  | `pnpm exec lint-staged`                                                                                                                               |
| commit-msg  | commitlint 校验 conventional 提交（含 `wip` 等扩展类型）                                                                                              |
| 编辑器      | [.editorconfig](.editorconfig)；[.vscode/settings.json](.vscode/settings.json)、[.vscode/extensions.json](.vscode/extensions.json) 推荐本仓库工具扩展 |

### 文档与发布

- **VitePress**：`pnpm docs:dev` / `docs:build` / `docs:preview`。
- **release-it**：`pnpm release`；`CHANGELOG.md` 由 conventional-changelog 插件维护；`requireBranch` 为 `main` / `master`；`npm.publish: false`。
- **OpenAPI → TS**：`pnpm gen:api` / `gen:api:remote`（需 `OPENAPI_URL`）。

### 部署与容器

- **多阶段 [Dockerfile](Dockerfile)**：Node 20 Alpine + pnpm 安装与 `pnpm run build --mode <BUILD_MODE>`，产物用 **nginx:1.27-alpine** 托管，自定义 [nginx.conf](nginx.conf)，健康检查等见 [docker-compose.yml](docker-compose.yml)。
- **环境文件**：如 `.env`、`.env.development`、`.env.staging`、`.env.production`（与 Vite `mode` 对应）。

### CI / CD 与质量门禁（.github）

| 工作流                                                               | 作用                                                                                                                                  |
| -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| [workflows/ci.yml](.github/workflows/ci.yml)                         | `push`/`pull_request` 到 `main`、`master`、`develop`：pnpm 安装、**pnpm audit**（high）、lint、stylelint、type-check、test:run、build |
| [workflows/deploy.yml](.github/workflows/deploy.yml)                 | 生产：依赖 CI 通过后 Docker 构建/推送与部署                                                                                           |
| [workflows/deploy-staging.yml](.github/workflows/deploy-staging.yml) | 预发（如 `develop` 或 `workflow_dispatch`）                                                                                           |
| [workflows/lighthouse.yml](.github/workflows/lighthouse.yml)         | PR 上 Lighthouse，配置 [.lighthouserc.json](.lighthouserc.json)                                                                       |

另含 Issue 模板、PR 模板等，见 `.github/`。

---

## 目录结构

```
.
├── .editorconfig
├── .env / .env.development / .env.staging / .env.production  # 本地勿提交含密钥；Vite 按 mode 加载
├── .eslintrc-auto-import.json    # unplugin-auto-import 供 ESLint 识别全局
├── .github/
│   ├── ISSUE_TEMPLATE/ …
│   ├── pull_request_template.md
│   └── workflows/                # ci、deploy、deploy-staging、lighthouse
├── .husky/                       # pre-commit：lint-staged；commit-msg：commitlint
├── .lighthouserc.json
├── .prettierignore
├── .release-it.json
├── .stylelintrc.json
├── .vscode/                      # 推荐设置与扩展
├── docs/                         # VitePress 文档
│   ├── .vitepress/config.ts
│   ├── index.md
│   ├── guide/                    # architecture、deploy、permission、theme 等
│   └── components/               # 组件说明文档
├── mock/                         # vite-plugin-mock（VITE_USE_MOCK=true）
│   ├── user.ts
│   └── menu.ts
├── openapi/
│   └── api.yaml                  # OpenAPI，pnpm gen:api 生成类型
├── public/                       # 静态资源，不经打包处理
│   └── favicon.svg
├── scripts/
│   └── gen-api.ts                # OpenAPI → src/types/api-schema.d.ts
├── src/
│   ├── App.vue
│   ├── main.ts
│   ├── api/
│   │   ├── index.ts
│   │   └── modules/              # 如 user、menu
│   ├── assets/styles/
│   │   ├── variables.scss        # 变量 + 全局 SCSS 注入
│   │   ├── dark.scss
│   │   ├── reset.scss
│   │   ├── index.scss
│   │   └── tokens.ts
│   ├── components/
│   │   ├── ErrorBoundary/
│   │   ├── PageContainer/
│   │   ├── ProTable/
│   │   ├── Skeleton/
│   │   └── SvgIcon/
│   ├── composables/              # usePermission、useTable、useRequest、useMessage、useECharts 等
│   ├── directives/               # v-permission、v-role
│   ├── enums/
│   ├── locales/                  # i18n，setupI18n
│   ├── plugins/                  # Element Plus、errorHandler、Sentry、I18n
│   ├── router/
│   │   ├── index.ts
│   │   └── guards.ts
│   ├── stores/
│   │   ├── index.ts
│   │   └── modules/              # app、user、permission、tabs
│   ├── types/
│   │   ├── api.d.ts
│   │   ├── api-schema.d.ts       # 代码生成
│   │   ├── auto-imports.d.ts
│   │   ├── components.d.ts
│   │   ├── env.d.ts
│   │   ├── global.d.ts
│   │   └── router.d.ts
│   ├── utils/                    # http、storage、format、common、echarts、performance
│   └── views/
│       ├── auth/
│       │   ├── register/
│       │   └── forgot/
│       ├── examples/             # crud、form、upload、charts
│       ├── home/
│       ├── layout/               # 含 Header、Sidebar、Tabs、Main、SidebarItem 等
│       ├── login/
│       └── error/                # 403、404
├── CHANGELOG.md
├── CONTRIBUTING.md
├── Dockerfile
├── docker-compose.yml
├── eslint.config.mjs
├── commitlint.config.ts
├── index.html
├── nginx.conf
├── package.json
├── pnpm-lock.yaml
├── prettier.config.mjs
├── tsconfig.json / tsconfig.app.json / tsconfig.node.json
├── vite.config.ts
├── vitest.config.ts
└── 架构.md                       # 本文件
```

（说明：`main.ts` 在 `bootstrap` 外先引入 Element Plus 全量 CSS、`dark/css-vars.css` 与 `./assets/styles/index.scss`，顺序见源码注释。）

---

## 启动流程（`src/main.ts` → `bootstrap()`）

```
bootstrap()
  1. setupStore(app)              — Pinia + pinia-plugin-persistedstate
  2. registerDirectives(app)      — v-permission / v-role
  3. app.use(router)              — 先挂路由（Sentry 等需 router 实例）
  4. setupPlugins(app, router)
     └─ setupErrorHandler         — Vue / window 全局错误
     └─ setupElementPlus          — 图标等
     └─ setupI18n                 — vue-i18n
     └─ setupSentry               — 可选，无 DSN 则跳过
  5. await router.isReady()       — 首屏前执行完相关守卫/解析
  6. app.mount('#app')
  7. initWebVitals()              — CLS / INP / LCP / FCP / TTFB
```

## 路由流程（`src/router/guards.ts` 摘要）

```
beforeEach: NProgress.start
  无 token
    白名单路径（/login, /register, /forgot-password）或 meta.requiresAuth === false → 放行
    否则 → /login?redirect=…
  有 token 且目标为 /login → /
  有 token 但 userInfo 未加载 → fetchUserInfo，失败则 logout 并回登录
  动态路由未加载（!permissionStore.isRoutesLoaded）
    → generateRoutes() → 对每个动态路由 router.addRoute('Layout', route) → return { ...to, replace: true }
  合并 matched 的 meta.permissions，若非空且用户无任一对应权限（userStore.hasPermission）→ /403
  否则 → 放行
（RouteMeta 中的 meta.roles 已声明，当前 beforeEach 未做角色路由校验；按钮/区块可用 v-role 或 usePermission().hasRole()。）
afterEach:
  设置 document.title
  tabsStore.addTab(to)
  NProgress.done
onError: NProgress.done + 控制台错误
```

## HTTP 层特性（`src/utils/http`）

- **401**：若存在 `refreshToken` 则单飞刷新，队列中请求刷新完成后带新 `Authorization` 重放；无 `refreshToken` 或刷新失败时走登录过期确认框（`skipAuthRefresh` 可关闭整套逻辑以避开递归）。
- **去重与取消**：`cancelDuplicate: true` 时同 `requestKey`（默认 `METHOD:url`）取消进行中的旧请求，基于 `AbortController` 与内部 `pendingRequests` Map；导出 `cancelRequest` / `cancelAllRequests`。
- **重试**：`retryCount` > 0 且 HTTP 状态 ≥ 500 时指数退避后重发（**401/403 不参与**）。
- **成功判断**：`ResponseData` 的 `code` 须等于 `getApiSuccessCode()`（`config`）。
- **提示与 Loading**：`showError` 默认 `true`；`showLoading: true` 时配对 `showLoading`/`hideLoading`（`loading.ts` 内计数）。
- **GET**：自动追加 `_t` 时间戳防缓存（除非业务另行处理）。

## 权限体系

| 层级           | 机制                                                                                                                                |
| -------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 页面级         | 路由 `beforeEach` 中校验 `meta.permissions`（`userStore.hasPermission`；`permissions` 含 `*:*:*` 为超管时 `hasPermission` 恒 true） |
| 路由 meta 角色 | `RouteMeta.roles` 已定义，**当前路由守卫不校验**；需页面或组件内自判                                                                |
| 按钮级         | `v-permission` / `v-role` 与 `usePermission()`（`hasPermission` / `hasRole` 等）                                                    |

## 暗黑模式

- `appStore.setTheme('light' \| 'dark' \| 'system')`；`system` 使用 `matchMedia('(prefers-color-scheme: dark)')` 与 `change` 监听，并维护 `html` 的 `dark` 类；切换模式时会移除旧的 `system` 监听器（见 `app.ts`）。
- 样式侧配合 `assets/styles/variables.scss`、`dark.scss` 与 `element-plus/theme-chalk/dark/css-vars.css`（`main.ts` 引入顺序在注释中已说明）。

## 代码规范与脚本速查

| 脚本 / 文件                                  | 说明                                                                   |
| -------------------------------------------- | ---------------------------------------------------------------------- |
| `pnpm dev` / `build` / `preview`             | 开发、生产构建、预览 `dist`                                            |
| `pnpm type-check`                            | vue-tsc 无发出检查                                                     |
| `pnpm lint` / `lint:fix`                     | ESLint                                                                 |
| `pnpm lint:style` / `lint:style:fix`         | Stylelint                                                              |
| `pnpm format`                                | Prettier 写回                                                          |
| `pnpm test` / `test:run` / `test:coverage`   | Vitest 与覆盖率                                                        |
| `pnpm docs:*`                                | VitePress                                                              |
| [eslint.config.mjs](eslint.config.mjs)       | ESLint 9 flat、TS、Vue、Prettier 兼容层                                |
| [prettier.config.mjs](prettier.config.mjs)   | 如 `printWidth: 120`、`semi: false`、`singleQuote: true`（以文件为准） |
| [commitlint.config.ts](commitlint.config.ts) | conventional + `type-enum` 含 `wip`、`header-max-length: 100`          |

---

_版本号以 [package.json](package.json) 与锁文件为准；升级依赖后请同步更新本表。_
