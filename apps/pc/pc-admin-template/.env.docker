# Docker 静态镜像（docker compose 默认 BUILD_MODE=docker）：浏览器只请求同源 /api，由 nginx 反代到宿主机后端
# 后端默认见 docker/nginx/admin.conf 中的 proxy_pass（一般为宿主机 :3000）
VITE_API_BASE_URL=
VITE_API_PREFIX=/api
VITE_USE_MOCK=false
